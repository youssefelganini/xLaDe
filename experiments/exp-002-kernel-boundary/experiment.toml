id = "EXP-002"

name = "Kernel Boundary Violation Detection"

type = "script-policy"

status = "active"

allowed_modes = ["experimental"]

# Environment (not critical here but keep consistent)
lean_toolchain = "leanprover/lean4:stable"

# Execution
entry = "scripts/experiments/check-kernel.sh"

description = "Detects modifications to lean-core and enforces kernel immutability."