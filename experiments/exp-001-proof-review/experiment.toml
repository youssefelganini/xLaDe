id = "EXP-001"

name = "Enforced Proof Review"

type = "lean-policy"

status = "active"

allowed_modes = ["experimental"]

# Environment (v1.4.0 feature)
lean_toolchain = "leanprover/lean4:stable"

# Execution
entry = "policy.lean"

description = "Enforces proof review discipline using Lean-based checks."